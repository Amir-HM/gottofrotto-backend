# Database Configuration
# For local development:
DATABASE_URL=postgresql://localhost/gottofrotto_dev

# For production (Neon):
# DATABASE_URL=postgresql://username:password@hostname:port/database?sslmode=require

# CORS Configuration
STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:9000,http://localhost:5173,https://docs.medusajs.com
AUTH_CORS=http://localhost:8000,http://localhost:5173,https://docs.medusajs.com

# Redis (optional - uses in-memory if not provided)
# REDIS_URL=redis://localhost:6379

# Security
JWT_SECRET=supersecret-change-in-production
COOKIE_SECRET=supersecret-change-in-production
